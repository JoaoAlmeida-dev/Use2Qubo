{
  "decision_var_associations": ["RouteStop", "AssignedTo"],
  "decision_vars": [
    {
      "type": "link",
      "association": "RouteStop",
      "domain": ["Route", "Node"],
      "index_attribute": "step"
    },
    {
      "type": "link",
      "association": "AssignedTo",
      "domain": ["Route", "Truck"]
    }
  ],
  "objective": {
    "expression": "Route.allInstances->collect(r | r.edgeCost())->sum() + 1000 * GarbageBin.allInstances->collect(b | b.coveragePenalty())->sum() + 1000 * Route.allInstances->collect(r | r.shapePenalty())->sum()",
    "minimise": true
  }
}
